# Session Handoff — 2026-06-19

## What was done this session

- Explored the full codebase and documented the existing state (Next.js 15, React 19, Tailwind CSS v4, 20 UI components, 8 routes, static book list in `/lib/library.ts`)
- Researched two reference libraries: designisaparty.com (Notion + Supabase, cover grid) and scottboms.com (Kirby CMS, masonry grid, 762 books)
- Created and approved a full build plan covering content, library CMS, work section, resume redirect, git strategy, testing, and deployment
- Decided **no Supabase needed** — Open Library Covers API by ISBN replaces manual cover uploads; Supabase can be added later via a `CoverURL` override field if needed
- Created `/project-context` skill at `~/.claude/skills/project-context/SKILL.md` — generates structured context docs from any project input
- Generated `docs/project-context.md` from the build plan; includes ICP (hiring managers + design recruiters), open questions, and gaps
- Committed and pushed `docs/project-context.md` to `main`

---

## Where to pick up

**Next immediate steps (in order):**

1. **Create the Notion database** — Use the Notion MCP to create a new database with this schema:

   | Property | Type |
   |---|---|
   | Title | Title |
   | Author | Text |
   | Category | Select: Design / Systems / Craft & career / Adjacent |
   | Year | Number |
   | Status | Select: Reading / Read / Wishlist |
   | Note | Text |
   | ISBN | Text |
   | Curated | Checkbox |
   | Color | Text |

2. **Migrate 9 existing books** from `/lib/library.ts` into that Notion database

3. **Get the Notion API key + DB ID** and add to `.env.local`:
   ```
   NOTION_API_KEY=
   NOTION_LIBRARY_DB_ID=
   NEXT_PUBLIC_SITE_URL=http://localhost:3000
   ```

4. **Create branch** `feature/library-notion` and begin implementation:
   - `lib/notion.ts` — typed Notion client + fetch functions
   - `app/api/notion-file/route.ts` — proxy for Notion file URLs (if needed)
   - `components/ui/BookGrid.tsx` + `BookCard.tsx` + `BookViewSwitcher.tsx`
   - Wire `app/library/page.tsx` to replace the static import

---

## Key decisions already made (do not re-litigate)

- **Library CMS:** Notion (not Contentful, not Supabase, not static)
- **Cover images:** Open Library by ISBN, color tile fallback, no Supabase at launch
- **Case studies:** MDX files in `/content/work/`, not a database
- **Resume:** `redirect()` in `app/resume/page.tsx` to a hosted PDF
- **Writing:** Stays placeholder
- **No dark mode** — intentional (astigmatism)
- **No em dashes** in any user-facing copy

---

## Open questions to resolve before building

1. Resume PDF — where is it hosted? (Needed before wiring the redirect)
2. Which 3–4 case studies are launching? Do any exist from the old portfolio?
3. Should `/work` feature one case study above the rest, or flat grid?
4. OG image strategy — nothing defined yet; needed before the site is shared publicly

---

## Key files

| File | Purpose |
|---|---|
| `docs/project-context.md` | Full project context with ICP, constraints, open questions |
| `~/.claude/plans/i-would-like-to-validated-pearl.md` | Approved build plan (all phases) |
| `~/.claude/skills/project-context/SKILL.md` | `/project-context` skill + embedded build plan |
| `lib/library.ts` | Current static book data (9 books — migrate to Notion) |
| `app/library/page.tsx` | Library page to rework |
| `app/work/` | Placeholder + scaffolded `[slug]` route |
| `app/resume/page.tsx` | Replace with redirect |
